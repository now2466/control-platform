# T05 안전 정지·수동 제어 TDD 기록

실제 ROS `cmd_vel`과 하드웨어 정지는 T12 범위다. 이 단계의 상태는 mock 관측 계약이며 물리 안전 확인이 아니다.

| 테스트 | RED 명령 및 실제 실패 이유 | GREEN 명령 및 결과 |
|---|---|---|
| `test_operator_can_request_all_stop_without_lease_and_observe_each_target` | `cd backend && .venv/bin/python -m pytest tests/test_safety.py::test_operator_can_request_all_stop_without_lease_and_observe_each_target -q` → `POST /api/v1/stop` 미구현으로 `404 != 202`. | 같은 명령 → `1 passed`. operator가 lease 없이 전체 정지를 접수하고 각 대상 ACKNOWLEDGED 및 command 상태를 조회한다. |
| `test_stop_reset_requires_the_active_control_lease` | `cd backend && .venv/bin/python -m pytest tests/test_safety.py::test_stop_reset_requires_the_active_control_lease -q` → reset 경로 미구현으로 `404 != 409`. | `cd backend && .venv/bin/python -m pytest tests/test_safety.py -q` → `2 passed`. reset은 소유 세션의 유효 lease가 있어야 202 접수된다. |
| `test_teleop_socket_rejects_missing_lease_before_manual_input` | `cd backend && .venv/bin/python -m pytest tests/test_safety.py::test_teleop_socket_rejects_missing_lease_before_manual_input -q` → `/ws/teleop` 부재로 handshake close 1000. | `cd backend && .venv/bin/python -m pytest tests/test_safety.py -q` → `3 passed`. 인증된 socket도 유효 lease 없이 입력을 수락하지 않는다. |
| `test_stop_request_id_is_persisted_and_rejects_changed_payload` | command persistence 구현 뒤 추가한 characterization 테스트이므로 RED 실행은 없다. | `cd backend && .venv/bin/python -m pytest tests/test_safety.py -q` → `4 passed`. 동일 payload는 같은 command ID, 변경 payload는 409을 확인한다. |

프런트엔드 `T05.test.tsx`의 blur zero-stop은 genuine RED(blur 때 0 속도 패킷 누락) 후 GREEN으로 확인했다. stop 단일 전송/부분 상태와 hold cadence는 구현 뒤 characterization이다. `cd frontend && npm run test -- --run`은 27 passed, production build도 통과했다.

| `test_stop_confirmation_and_timeout_use_fake_monotonic_clock` | targeted pytest → `ModuleNotFoundError` (stop observation state machine 부재). | `1 passed`; fresh latch와 속도 기준 0.5초 CONFIRMED, 2초 UNCONFIRMED을 fake clock으로 확인. |
| `test_general_queue_caps_at_100_but_stop_bypasses_it` | targeted pytest → `ImportError: CommandQueue` (queue behavior 부재). | commands+safety targeted → `6 passed`; 일반 큐 100 제한과 stop priority를 확인. |
| `test_teleop_watchdogs_and_individual_stop_protect_both_robots` | targeted pytest → `ModuleNotFoundError` (manual watchdog state 부재). | `cd backend && .venv/bin/python -m pytest tests/test_commands.py -q` → `2 passed`; 300ms input/1s operational timeout과 protective dual stop을 fake clock으로 확인. |
| `test_routes_cap_normal_work_and_execute_priority_stop_first` | `cd backend && .venv/bin/python -m pytest tests/test_commands.py::test_routes_cap_normal_work_and_execute_priority_stop_first -q` → `TypeError: create_app() got an unexpected keyword argument 'start_command_worker'`. The route test could not pause a worker, so it could not prove queue capacity or priority. | Same command → `1 passed`. Actual `/robots/{id}/mode` requests fill the normal lane; request 101 is `503 QUEUE_FULL`; `/stop` remains `202` and is the first command executed when the dispatcher is released. |
| `test_runtime_watchdog_zeros_expired_teleop_without_latching_stop` | Characterization added with the lifecycle runtime wiring; no separate RED claim. | `cd backend && .venv/bin/python -m pytest tests/test_commands.py::test_runtime_watchdog_zeros_expired_teleop_without_latching_stop -q` → `1 passed`. A fake-clock 300ms manual-input expiry sends 0 velocity only to the selected robot; connection and stop-latch state remain unchanged. Websocket disconnect and lease expiry retain the separate protective-stop path. |

The production command paths for stop, stop reset, and robot mode now submit through `CommandDispatcher`; its lifespan worker owns adapter execution and durable state transitions. `GET /api/v1/commands/{id}` remains the polling contract used by the stop bar. The application watchdog calls `TeleopService.tick()` every 50ms and clears expired lease/session records during the same runtime tick.

| `test_stop_stays_running_until_observation_confirms_then_succeeds` | `cd backend && .venv/bin/python -m pytest tests/test_commands.py::test_stop_stays_running_until_observation_confirms_then_succeeds -q` → stop was marked `SUCCEEDED` immediately after mock adapter acceptance, before a state observation. | Same command → `1 passed`. It remains `RUNNING`; runtime ticks observe a stable stopped state for 0.5s, persist `CONFIRMED` targets, then mark it `SUCCEEDED`. |
| `test_rejected_stop_is_failed_with_an_unconfirmed_target` and `test_adapter_exception_marks_failure_and_worker_processes_the_next_command` | Added after review of the dispatcher completion path; the prior implementation either reported adapter rejection as success or let an exception escape the worker. | Targeted command tests pass. Rejection/exception persists `UNCONFIRMED` and `FAILED`; a subsequent queued command still completes. |

Stop adapter acceptance is now only an acknowledgement. Runtime state observation finalizes a stop as `SUCCEEDED` when every target is `CONFIRMED`, or `TIMED_OUT` when an observed target becomes `UNCONFIRMED`; explicit adapter rejection and adapter exceptions are `FAILED`.

| `test_request_id_is_idempotent_for_24_hours_but_keeps_expired_audit_history` | `cd backend && .venv/bin/python -m pytest tests/test_commands.py::test_request_id_is_idempotent_for_24_hours_but_keeps_expired_audit_history -q` → a changed mode payload incorrectly returned `202` because mode was omitted from the command payload hash. | Same command → `1 passed`. A fake UTC clock proves same payload is replayed and changed payload conflicts within 24h; exactly 24h later the same request ID creates a new command while the original audit row remains. |

Migration 003 drops the perpetual `(user_id, request_id)` unique index and adds a lookup index. `Storage.create_command` considers only records newer than 24 hours for idempotency; command rows remain immutable audit history.

| `test_concurrent_request_claims_are_atomic_for_same_and_changed_payload` | `cd backend && .venv/bin/python -m pytest tests/test_commands.py::test_concurrent_request_claims_are_atomic_for_same_and_changed_payload -q` → concurrent calls sharing the SQLite connection produced `sqlite3.InterfaceError: bad parameter or other API misuse`; one caller did not receive a claim result. | Same command → `1 passed`. A barrier releases two threads together: same payload yields one row and one shared command ID; changed payload yields exactly one success and one conflict. |
| `test_concurrent_expired_request_creates_one_new_generation_and_retains_audit` | Added as the expired-key concurrency companion; prior data model did not provide an atomic expired-key claim. | Targeted pytest → `1 passed`. At exactly 24h, concurrent callers create one new generation and preserve the old audit row. |

`create_command` now uses a storage mutex for the shared connection and `BEGIN IMMEDIATE` for the SQLite write claim, covering the lookup, payload comparison, 24h decision, and insert in one transaction. This protects the configured single-worker process and preserves database-level write serialization.
