function csrf() { return document.cookie.split('; ').find(v => v.startsWith('cc_csrf='))?.split('=')[1] ?? '' }
export async function stop(target: 'all'|'robot_1'|'robot_2') { const r = await fetch('/api/v1/stop', { method:'POST', credentials:'include', headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()}, body:JSON.stringify({request_id:crypto.randomUUID(),target}) }); if(!r.ok) throw new Error(`정지 요청 실패 (${r.status})`); return r.json() }
export async function command(commandId: string) { const r=await fetch(`/api/v1/commands/${commandId}`,{credentials:'include'}); if(!r.ok) throw new Error(`상태 조회 실패 (${r.status})`); return r.json() }
export async function resetStop(target: 'all'|'robot_1'|'robot_2', lease_id: string) { const r=await fetch('/api/v1/stop/reset',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()},body:JSON.stringify({request_id:crypto.randomUUID(),target,lease_id})}); if(!r.ok) throw new Error(`정지 해제 실패 (${r.status})`); return r.json() }
export async function setMode(robotId: string, mode: 'IDLE'|'AUTO'|'FOLLOW'|'MANUAL'|'STOPPED') { const r=await fetch(`/api/v1/robots/${robotId}/mode`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()},body:JSON.stringify({request_id:crypto.randomUUID(),mode})}); if(!r.ok) throw new Error(`모드 전환 실패 (${r.status})`); return r.json() }
