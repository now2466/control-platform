export type FrameMetadata = { frame_id: string; captured_at: string | null; received_at: string; width: number; height: number }
export function decodeFrame(data: ArrayBuffer): { metadata: FrameMetadata; jpeg: Uint8Array } { if (data.byteLength < 5) throw new Error('frame too short'); const view = new DataView(data); const length = view.getUint32(0); if (length > 65536 || length + 4 >= data.byteLength) throw new Error('invalid metadata length'); const metadata = JSON.parse(new TextDecoder().decode(new Uint8Array(data, 4, length))) as FrameMetadata; const jpeg = new Uint8Array(data, 4 + length); if (!metadata.frame_id || metadata.width <= 0 || metadata.height <= 0 || jpeg.length < 4 || jpeg[0] !== 255 || jpeg[1] !== 216 || jpeg[jpeg.length - 2] !== 255 || jpeg[jpeg.length - 1] !== 217) throw new Error('invalid frame'); return { metadata, jpeg }
}
